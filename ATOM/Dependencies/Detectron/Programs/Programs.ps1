$programs = [ordered]@{
	'Remote Access' = [ordered]@{
		'Alpemix' = @{
			folder = $null
			process = $null
		}
		'AnyDesk' = @{
			folder = 'AnyDesk'
			process = 'anydesk.exe'
		}
		'AweSun' = @{
			folder = 'AweRay'
			process = $null
		}
		'GlanceGuest' = @{
			folder = 'GlanceGuest'
			process = $null
		}
		'GoToAssist' = @{
			folder = $null
			process = $null
		}
		'LogMeIn' = @{
			folder = $null
			process = $null
		}
		'OptimumDesk' = @{
			folder = $null
			process = $null
		}
		'Optimum Desk' = @{
			folder = $null
			process = $null
		}
		'Remote Assistance' = @{
			folder = $null
			process = $null
		}
		'RemotePC' = @{
			folder = $null
			process = $null
		}
		'Remote Utilities' = @{
			folder = $null
			process = $null
		}
		'Splashtop' = @{
			folder = 'Splashtop'
			process = 'splashtop.exe'
		}
		'Supremo' = @{
			folder = 'Supremo'
			process = 'supremo.exe'
		}
		'TeamViewer' = @{
			folder = 'TeamViewer'
			process = 'teamviewer.exe'
		}
		'UltraViewer' = @{
			folder = 'UltraViewer'
			process = 'ultraviewer.exe'
		}
		'UltraVNC' = @{
			folder = 'UltraVNC'
			process = 'ultravnc.exe'
		}
		'VNC Connect' = @{
			folder = 'VNC Connect'
			process = 'vncconnect.exe'
		}
		'Zoho Assist' = @{
			folder = $null
			process = $null
		}
	}
	'Anti-Virus' = [ordered]@{
		'McAfee' = @{
			folder = 'McAfee'
			process = $null
		}
	}
	'Malware' = [ordered]@{
		'Advanced System Care' = @{ folder = $null; process = $null }
		'Altruistics' = @{ folder = $null; process = $null }
		'Avast Secure Browser' = @{ folder = $null; process = $null }
		'Avery Teoma Search App' = @{ folder = $null; process = $null }
		'BBWC' = @{ folder = $null; process = $null }
		'Bonefreeze' = @{ folder = $null; process = $null }
		'Browser Assistant' = @{ folder = $null; process = $null }
		'BrowserAssistant' = @{ folder = $null; process = $null }
		'Browser Extension' = @{ folder = $null; process = $null }
		'Clear' = @{ folder = $null; process = $null }
		'ClearBar' = @{ folder = $null; process = $null }
		'ClientPCSpeedup' = @{ folder = $null; process = $null }
		'Deepteep' = @{ folder = $null; process = $null }
		'Driver Easy' = @{ folder = $null; process = $null }
		'DriverEasy' = @{ folder = $null; process = $null }
		'Driver Support One' = @{ folder = $null; process = $null }
		'Driver Tonic' = @{ folder = $null; process = $null }
		'Driver Tuner' = @{ folder = $null; process = $null }
		'DriverUpdate' = @{ folder = $null; process = $null }
		'Easy Radio Player' = @{ folder = $null; process = $null }
		'EstimateSpeedUp' = @{ folder = $null; process = $null }
		'GetMyDrivers' = @{ folder = $null; process = $null }
		'iTop Easy Desktop' = @{ folder = $null; process = $null }
		'iTop VPN' = @{ folder = $null; process = $null }
		'iTop Screen Recorder' = @{ folder = $null; process = $null }
		'Malware Crusher' = @{ folder = $null; process = $null }
		'N9 Firewall' = @{ folder = $null; process = $null }
		'OneLaunch' = @{ folder = $null; process = $null }
		'OneStart' = @{ folder = $null; process = $null }
		'PC App Store' = @{ folder = $null; process = $null }
		'PDFHub' = @{ folder = $null; process = $null }
		'PremierOpinion' = @{ folder = $null; process = $null }
		'PrivDog' = @{ folder = $null; process = $null }
		'Quick Driver Updater' = @{ folder = $null; process = $null }
		'RelevantKnowledge' = @{ folder = $null; process = $null }
		'RestMinder' = @{ folder = $null; process = $null }
		'Restoro' = @{ folder = $null; process = $null }
		'screensearchutils' = @{ folder = $null; process = $null }
		'ScreensRecorder' = @{ folder = $null; process = $null }
		'Simple PC Optimizer' = @{ folder = $null; process = $null }
		'Solve iQ' = @{ folder = $null; process = $null }
		'SSOption' = @{ folder = $null; process = $null }
		'SlimCleaner Plus' = @{ folder = $null; process = $null }
		'System Mechanic' = @{ folder = $null; process = $null }
		'Viewndow' = @{ folder = $null; process = $null }
		'WaveBrowser' = @{ folder = $null; process = $null }
		'WeatherApp' = @{ folder = $null; process = $null }
		'Web Companion' = @{ folder = $null; process = $null }
		'WebDiscover Browser' = @{ folder = $null; process = $null }
		'WebBar Toolbar' = @{ folder = $null; process = $null }
		'WinZip' = @{ folder = $null; process = $null }
	}
	'Bloatware' = [ordered]@{
	# General Manual Uninstallers
		'Google Toolbar for Internet Explorer' = @{ folder = $null; process = $null }
		'Windows PC Health Check' = @{ folder = $null; process = $null }
	# Acer OEM Manual Uninstallers
	# ASUS OEM Manual Uninstallers
		'ASUS AI Recovery' = @{ folder = $null; process = $null }
		'ASUS FancyStart' = @{ folder = $null; process = $null }
		'ASUS Live Update' = @{ folder = $null; process = $null }
		'ASUS Power4Gear Hybrid' = @{ folder = $null; process = $null }
		'ASUS SmartLogon' = @{ folder = $null; process = $null }
		'ASUS Splendid Video Enhancement Technology' = @{ folder = $null; process = $null }
		'ASUS Virtual Camera' = @{ folder = $null; process = $null }
		'ASUS WebStorage' = @{ folder = $null; process = $null }
		'ASUS Welcome' = @{ folder = $null; process = $null }
		'AsusVibe' = @{ folder = $null; process = $null }
	# Dell OEM Manual Uninstallers
		'Dell Customer Connect' = @{ folder = $null; process = $null }
		'Dell Digital Delivery' = @{ folder = $null; process = $null }
		'Dell Digital Delivery Services' = @{ folder = $null; process = $null }
		'Dell Help & Support' = @{ folder = $null; process = $null }
		'Dell Mobile Connect Driver' = @{ folder = $null; process = $null }
		'Dell Product Registration' = @{ folder = $null; process = $null }
		'Dell Shop' = @{ folder = $null; process = $null }
		'Fusion Service' = @{ folder = $null; process = $null }
		'QuickSet64' = @{ folder = $null; process = $null }
		'SmartByte' = @{ folder = $null; process = $null }
		'SmartByte Drivers & Services' = @{ folder = $null; process = $null }
	# HP OEM Manual Uninstallers
		'Bonjour' = @{ folder = $null; process = $null }
		'Duet Display' = @{ folder = $null; process = $null }
		'ExpressVPN' = @{ folder = $null; process = $null }
		'HP Documentation' = @{ folder = $null; process = $null }
		'HP Connection Optimizer' = @{ folder = $null; process = $null }
		'HP Jumpstart Apps' = @{ folder = $null; process = $null }
		'HP Jumpstart Bridge' = @{ folder = $null; process = $null }
		'HP Jumpstart Launch' = @{ folder = $null; process = $null }
		'WildTangent Games' = @{ folder = $null; process = $null }
		'WildTangent Helper' = @{ folder = $null; process = $null }
		'WildTangent Shortcut Provider' = @{ folder = $null; process = $null }
	# Lenovo OEM Manual Uninstallers
		'Lenovo Smart Appearance Components' = @{ folder = $null; process = $null }
		'Lenovo Voice Service' = @{ folder = $null; process = $null }
		'Lenovo Welcome' = @{ folder = $null; process = $null }
		'Smart Note' = @{ folder = $null; process = $null }
	# MSI OEM Manual Uninstallers
	# Razer OEM Manual Uninstallers
	# Samsung OEM Manual Uninstallers
		'BatteryLifeExtender' = @{ folder = $null; process = $null }
		'Easy Content Share' = @{ folder = $null; process = $null }
		'Easy Display Manager' = @{ folder = $null; process = $null }
		'Easy Display Manager Option' = @{ folder = $null; process = $null }
		'Easy Migration' = @{ folder = $null; process = $null }
		'Easy Network Manager' = @{ folder = $null; process = $null }
		'Easy Network Manager Help' = @{ folder = $null; process = $null }
		'EasyFileShare' = @{ folder = $null; process = $null }
		'Fast Start' = @{ folder = $null; process = $null }
		'Movie Color Enhancer' = @{ folder = $null; process = $null }
		'Movie Color Enhancer Option' = @{ folder = $null; process = $null }
		'Samsung Support Center' = @{ folder = $null; process = $null }
		'Samsung Update Plus' = @{ folder = $null; process = $null }
		'Samsung Update Plust Help' = @{ folder = $null; process = $null }
	}
}